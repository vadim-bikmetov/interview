## Git basic
<img src="https://raw.githubusercontent.com/vadim-bikmetov/interview/main/images/git.png" width="200" alt="git">
1. Что такое GitFlow?

<details>
  <summary>Ответ</summary>

GitFlow - модель ветвления Git.

*Ключевые идеи*:
1. Данная модель отлично подходит для организации рабочего процесса на основе релизов,
2. Gitflow предлагает создание отдельной ветки для исправлений ошибок в продуктовой среде.

*Последовательность работы при использовании модели Gitflow*:

1. Из *master* создается ветка *develop*.
2. Из *develop* создаются ветки *feature*.
3. Когда разработка новой функциональности завершена, она объединяется с веткой *develop*.
4. Из *develop* создается ветка *release*.
5. Когда ветка релиза готова, она объединяется с *develop* и *master*.
6. Если в *master* обнаружена проблема, из нее создается ветка *hotfix*.
7. Как только исправление на ветке *hotfix* завершено, она объединяется с *develop* и *master*.

</details>

---

2. Чем `merge` отличается от `rebase`?

<details>
  <summary>Ответ</summary>

- `git merge` - выполняет слияние коммитов из одной ветки в другую. В этом процессе изменяется только целевая ветка. История исходных веток остается неизменной.

<img src="https://raw.githubusercontent.com/vadim-bikmetov/interview/main/images/git/git-merge.png" width="700" alt="git">

  *Преимущества*:
    1. Простота,
    2. Сохраняет полную историю и хронологический порядок,
    3. Поддерживает контекст ветки.

  *Недостатки*:
    1. История коммитов может быть заполнена (загрязнена) множеством коммитов,
    2. Отладка с использованием git bisect может стать сложнее.


- `git rebase` - сжимает все изменения в один патч. Затем интегрирует патч в целевую ветку. В отличии от *merge*, *rebase* перезаписывает историю, потому что она передаётся завершенную работу из одной ветки в другую. В процессе устраняется нежелательная история.

<img src="https://raw.githubusercontent.com/vadim-bikmetov/interview/main/images/git/git-rebase.png" width="700" alt="git">

  *Преимущества*:
    1. Упрощает потенциально сложную историю,
    2. Упрощение манипуляций с единственным коммитом,
    3. Избежание слияния коммитов в занятых репозиториях и ветках,
    4. Очищает промежуточные коммиты, делая их одним коммитом, что полезно для DevOps команд.

    *Недостатки*:
    1. Сжатие фич до нескольких коммитов может скрыть контекст
    2. Перемещение публичных репозиториев может быть опасным при     работе в команде,
    3. Появляется больше работы,
    4. Для восстановления с удаленными ветками требуется     принудительный пуш. Это приводит к обновлению всех веток, имеющих одно и то же имя, как локально, так и удаленно.

</details>

---

3. Чем `tag` отличается от `branch`?

<details>
  <summary>Ответ</summary>

И *tag* и *branch* представляют собой указатели на коммиты.
- Ветка представляет собой отдельный поток разработки, который может выполняться одновременно с другими разработками в той же кодовой базе. Коммит в ветке указывает на изменения, которые добавляются в новых коммитах
- Тег представляет собой версию определенной ветки в определенный момент времени.

*Tag* представляет собой версию той или иной ветки в определенный момент времени. *Branch* представляет собой отдельный поток разработки, который может выполнятся одновременно с другими разработками в той же кодовой базе.

</details>

---

4. В ветке *develop* есть коммит с изменениями, которые нужно перенести в ветку *master*. Как это сделать?

<details>
  <summary>Ответ</summary>

Необходимо найти хеш этого коммита и выполнить следующую комманду в ветке, в которую нужно перенести коммит.
```sh
git cherry-pick <commit_hash>
```

</details>

---

5. Для чего нужна команда `git commit --amend`?

<details>
  <summary>Ответ</summary>

`commit --ammend` используется для исправления сообщения последнего коммита. Также возможно использовать, чтобы добавить файлы в индекс (`git add`), после добавить файлы в коммит `git commit --ammend`.

</details>

---

6. Что такое Trunk-based development?

<details>
  <summary>Ответ</summary>

Trunk-based Development (TBD) - модель ветвления, в которой разработчики совместно работают над кодом в одной ветви, называемой "стволом" (trunk). При этом другие ветви имеют короткий срок жизни благодаря использованию документированных методов.

</details>

---

7. Состояние репозитория ушло на много коммитов вперед. Как откатить весь репозиторий к определенному коммиту?

<details>
  <summary>Ответ</summary>

git reset --hard <tag/branch/commit hash>

</details>

---

8. В репозиторий запушен коммит с изменениями в двух файлах. Как откатить изменения этого коммита?

<details>
  <summary>Ответ</summary>

git revert <commit hash>

</details>

---

9. Что такое Git и для чего он используется?
<details>
  <summary>Ответ</summary>

Git — это распределенная система контроля версий, которая позволяет отслеживать изменения в файлах и координировать работу нескольких людей над одним проектом.
</details>

---
10. В чем разница между Git и GitHub?
<details>
  <summary>Ответ</summary>

Git — это система контроля версий, а GitHub — веб-сервис для хостига Git-репозиториев с дополнительными функциями для collaboration.
</details>

---
11. Что такое репозиторий в Git?
<details>
  <summary>Ответ</summary>

Репозиторий — это хранилище проекта, содержащее все файлы и историю их изменений.
</details>

---
12. Как создать новый репозиторий в Git?
<details>
  <summary>Ответ</summary>

Команда `git init` создает новый локальный репозиторий в текущей директории.
</details>

---
13. Что такое коммит (commit) в Git?
<details>
  <summary>Ответ</summary>

Коммит — это snapshot изменений в репозитории в определенный момент времени с комментарием.
</details>

---
14. Как добавить файлы в staging area?
<details>
  <summary>Ответ</summary>

Команда `git add <filename>` добавляет конкретный файл, а `git add .` добавляет все измененные файлы.
</details>

---
15. Как сделать коммит в Git?
<details>
  <summary>Ответ</summary>

Команда `git commit -m "сообщение"` создает коммит с описанием изменений.
</details>

---
16. Что такое ветка (branch) в Git?
<details>
  <summary>Ответ</summary>

Ветка — это независимая линия разработки, позволяющая работать над разными features одновременно.
</details>

---
17. Как создать новую ветку?
<details>
  <summary>Ответ</summary>

Команда `git branch <branch_name>` создает новую ветку, `git checkout -b <branch_name>` создает и переключается на нее.
</details>

---
18. Что такое merge в Git?
<details>
  <summary>Ответ</summary>

Merge — это процесс объединения изменений из одной ветки в другую.
</details>

---
19. Как выполнить слияние веток?
<details>
  <summary>Ответ</summary>

Переключиться на целевую ветку и выполнить `git merge <source_branch>`.
</details>

---
20. Что такое конфликт слияния и как его разрешить?
<details>
  <summary>Ответ</summary>

Конфликт возникает когда Git не может автоматически объединить изменения. Нужно вручную отредактировать конфликтующие файлы, затем добавить и закоммитить их.
</details>

---
21. Что такое remote repository?
<details>
  <summary>Ответ</summary>

Удаленный репозиторий — это версия проекта, хранящаяся на сервере (например, GitHub, GitLab).
</details>

---
22. Как добавить удаленный репозиторий?
<details>
  <summary>Ответ</summary>

Команда `git remote add origin <url>` добавляет удаленный репозиторий с именем origin.
</details>

---
23. Как отправить изменения в удаленный репозиторий?
<details>
  <summary>Ответ</summary>

Команда `git push origin <branch_name>` отправляет коммиты в удаленный репозиторий.
</details>

---
24. Как получить изменения из удаленного репозитория?
<details>
  <summary>Ответ</summary>

Команда `git pull origin <branch_name>` забирает и сливает изменения из удаленного репозитория.
</details>

---
25. В чем разница между git pull и git fetch?
<details>
  <summary>Ответ</summary>

`git fetch` только загружает изменения, а `git pull` загружает и сразу сливает их с текущей веткой.
</details>

---
26. Что такое .gitignore файл?
<details>
  <summary>Ответ</summary>

Файл .gitignore содержит шаблоны файлов и папок, которые Git должен игнорировать и не отслеживать.
</details>

---
27. Как отменить изменения в рабочей директории?
<details>
  <summary>Ответ</summary>

Команда `git checkout -- <filename>` отменяет изменения в конкретном файле.
</details>

---
28. Как отменить индексацию файла (unstage)?
<details>
  <summary>Ответ</summary>

Команда `git reset HEAD <filename>` убирает файл из staging area.
</details>

---
29. Как изменить последний коммит?
<details>
  <summary>Ответ</summary>

Команда `git commit --amend` позволяет изменить сообщение или добавить файлы в последний коммит.
</details>

---
30. Что такое rebase в Git?
<details>
  <summary>Ответ</summary>

Rebase — это альтернатива merge, которая перестраивает историю коммитов, создавая линейную историю.
</details>

---
31. В чем разница между merge и rebase?
<details>
  <summary>Ответ</summary>

Merge сохраняет историю всех веток, а rebase перезаписывает историю, создавая линейную последовательность коммитов.
</details>

---
32. Что такое stash в Git?
<details>
  <summary>Ответ</summary>

Stash позволяет временно сохранить незакоммиченные изменения, чтобы переключиться на другую ветку.
</details>

---
33. Как использовать git stash?
<details>
  <summary>Ответ</summary>

`git stash` сохраняет изменения, `git stash pop` восстанавливает последние сохраненные изменения.
</details>

---
34. Что такое tag в Git?
<details>
  <summary>Ответ</summary>

Tag — это метка для определенного коммита, часто используется для отметки версий (v1.0, v2.0).
</details>

---
35. Как создать тег?
<details>
  <summary>Ответ</summary>

`git tag -a v1.0 -m "версия 1.0"` создает аннотированный тег, `git push origin --tags` отправляет теги на remote.
</details>

---
36. Что такое git log и какие у него опции?
<details>
  <summary>Ответ</summary>

`git log` показывает историю коммитов. Опции: `--oneline`, `--graph`, `--author`, `--since` и др.
</details>

---
37. Как просмотреть изменения в коммите?
<details>
  <summary>Ответ</summary>

`git show <commit_hash>` показывает изменения в конкретном коммите.
</details>

---
38. Что такое cherry-pick?
<details>
  <summary>Ответ</summary>

Cherry-pick позволяет применить конкретный коммит из одной ветки в другую.
</details>

---
39. Как использовать git cherry-pick?
<details>
  <summary>Ответ</summary>

`git cherry-pick <commit_hash>` применяет указанный коммит к текущей ветке.
</details>

---
40. Что такое git bisect?
<details>
  <summary>Ответ</summary>

Git bisect помогает найти коммит, в котором была引入ена ошибка, используя бинарный поиск.
</details>

---
41. Как использовать git bisect?
<details>
  <summary>Ответ</summary>

`git bisect start`, затем отмечать коммиты как good/bad пока Git не найдет проблемный коммит.
</details>

---
42. Что такое submodule в Git?
<details>
  <summary>Ответ</summary>

Submodule позволяет включить один Git-репозиторий как поддиректорию в другом репозитории.
</details>

---
43. Как добавить submodule?
<details>
  <summary>Ответ</summary>

`git submodule add <repository_url> <path>` добавляет submodule в проект.
</details>

---
44. Что такое git hook?
<details>
  <summary>Ответ</summary>

Git hooks — это скрипты, которые автоматически выполняются при определенных событиях в Git (pre-commit, pre-push и др.).
</details>

---
45. Как настроить git hook?
<details>
  <summary>Ответ</summary>

Скрипты размещаются в директории .git/hooks/ и должны быть исполняемыми.
</details>

---
46. Что такое squash commit?
<details>
  <summary>Ответ</summary>

Squash объединяет несколько коммитов в один, упрощая историю изменений.
</details>

---
47. Как сделать squash коммитов?
<details>
  <summary>Ответ</summary>

`git rebase -i HEAD~n` где n - количество коммитов, затем отметить коммиты как squash.
</details>

---
48. Что такое git reflog?
<details>
  <summary>Ответ</summary>

Reflog показывает историю всех действий в репозитории, включая "потерянные" коммиты.
</details>

---
49. Как восстановить удаленную ветку с помощью reflog?
<details>
  <summary>Ответ</summary>

Найти коммит в `git reflog`, затем создать новую ветку на основе этого коммита.
</details>

---
50. Что такое git worktree?
<details>
  <summary>Ответ</summary>

Worktree позволяет работать с несколькими ветками одновременно в разных рабочих директориях.
</details>

---
51. Как переименовать ветку?
<details>
  <summary>Ответ</summary>

# Переименовать текущую ветку
`git branch -m [new-name]`

# Если мы находимся в любой ветке
`git branch -m [old-name] [new-name]`
</details>

---
52. Как удалить файл/папку из репозитория?
<details>
  <summary>Ответ</summary>

`git rm -r --cached [file/folder]`
</details>

---
53. Как объединить несколько коммитов в один цельный коммит?
<details>
  <summary>Ответ</summary>

Открываем интерактивное меню rebase
`git rebase -i HEAD~[число коммитов]`
# Проставляем s(squash)

Заливаем все изменения
`git push --force`
</details>

---
54. За что отвечает команда git bisect?
<details>
  <summary>Ответ</summary>

Для выполнения поиска коммита сломавшего логику
```
git bisect start
git bisect bad
git bisect good [хеш коммита]
```
</details>

---
55. Как настроить репозиторий для запуска инструментов проверки работоспособности кода непосредственно перед выполнением коммитов?
<details>
  <summary>Ответ</summary>

Это делается с помощью хука pre-commit. Для этого нужно прописать код проверки в конфигурационном файле pre-commit (.git/hooks). Git будет запускать данную проверку перед каждым коммитом. Один из популярных - husky.
</details>

---
56. Что такое tag в репозитории?
<details>
  <summary>Ответ</summary>

Способ поставить какую-то точку в истории git.

Создать тег
`git tag [name]`

Создать аннотированный тег
`git tag -a [name] [-m "Описание тега"]`
</details>

---
57. Как поменять ссылку на удаленный репозиторий?
<details>
  <summary>Ответ</summary>

Посмотреть текущие ссылки на удаленный репозиторий
`git remote -v`

Добавить ссылку на удаленный репозиторий
`git remote add origin [url_of_git_repository]`

Изменить ссылку на удаленный репозиторий
`git remote set-url origin [newurl_of_git_repository]`
</details>

---
58. Как можно отменить коммит, если он не был опубликован?
<details>
  <summary>Ответ</summary>

Вариант 1. В интерактивном режиме выполнить команду drop
`git rebase -i`

Вариант 2.
```
git reset HEAD~
git add --all
git reset --hard
```
</details>

---
59. В чем заключается разница между git pull и git fetch?
<details>
  <summary>Ответ</summary>

Извлекает все изменения текущей ветки с сервера
`git pull`

Получает все отсутствующие данные с удаленного репозитория, но не сливает их
`git fetch`
</details>

---
60. Что такое stage/unstage или index?
<details>
  <summary>Ответ</summary>

Области файлов, которые готовы к следующему коммиту или еще не готовы.
</details>

---
61. За что отвечает команда git stash?
<details>
  <summary>Ответ</summary>

Команда которая сохраняет текущие не закоммиченные изменения в локальном хранилище.

Показывает список всех stash записей
`git stash list`

Сохранить изменения в stash
`git stash`
`git stash save [name_in_stash_list]`

Выгружает изменения из stash в текущую ветку для работы, удаляя запись
`git stash pop`

Выгружает изменения из stash в текущую ветку для работы, но не удаляет запись
`git stash apply [--index [index]]`

Удаляет запись из stash 
`git stash drop`
Удаляет все наборы отложенных изменений
`git stash clear`
</details>

---
62. За что отвечает команда git rebase?
<details>
  <summary>Ответ</summary>

Изменяет историю коммитов, добавляя коммиты в конец ветки, изменяя только хеш коммитов. Нужно очень грамотно использовать в больших командах.

`git rebase [branch_name]`
</details>

---
63. За что отвечает команда git merge?
<details>
  <summary>Ответ</summary>

Загрязняет ветку мерж коммитами, но не изменяет историю коммитов.

`git merge [branch_name]`
</details>

---
64. Какие модели ветвления вы знаете?
<details>
  <summary>Ответ</summary>

GitFlow - есть две основные ветки main и develop.

GitHubFlow - есть основная ветка main.
</details>

---
65. Как создать репозиторий?
<details>
  <summary>Ответ</summary>

Инициализировать локально
`git init`

Клонировать удаленный репозиторий
`git clone [url]`
</details>

---
66. Из чего состоит коммит?
<details>
  <summary>Ответ</summary>

Это односвязанный список, который состоит из объектов с измененными файлами и ссылки на предыдущий коммит. Так же указаны имя автора, метки времени и сообщение коммита.
</details>

---
67. Каким образом можно установить было слияние ветки или нет?
<details>
  <summary>Ответ</summary>

`git branch --merged`
</details>

---
68. За что отвечает команда git config?
<details>
  <summary>Ответ</summary>

Позволяет работать с конфигурацией git репозитория.
</details>

---
69. Как найти список файлов, которые изменились в определенном коммите?
<details>
  <summary>Ответ</summary>

Загружаем список коммитов
`git log`

Открываем список изменения коммита
`git show [хеш коммита]`
</details>

---

# Провести ревизию