# 🐘 Вопросы по PostgreSQL для собеседования

Коллекция из 15 основных вопросов по PostgreSQL с подробными ответами для технических собеседований.

## 📚 Содержание

- [Базовые концепции](#базовые-концепции-)
- [Средний уровень](#средний-уровень-)
- [Продвинутый уровень](#продвинутый-уровень-)
- [Оптимизация и администрирование](#оптимизация-и-администрирование-)

---

## Базовые концепции 🟢

### 1. Что такое PostgreSQL и каковы его основные преимущества?
**Ответ:**
PostgreSQL - это мощная, открытая объектно-реляционная система управления базами данных. 

**Основные преимущества:**
- Поддержка ACID и транзакций
- Расширяемость и поддержка пользовательских типов данных
- Богатый набор встроенных типов данных (JSON, массивы, диапазоны)
- Поддержка полнотекстового поиска
- Высокая производительность и надежность

### 2. В чем основное различие между CHAR(n), VARCHAR(n) и TEXT?
**Ответ:**
- **CHAR(n)** - строка фиксированной длины. Если строка короче, дополняется пробелами
- **VARCHAR(n)** - строка переменной длины с ограничением максимальной длины
- **TEXT** - строка переменной длины без ограничений, рекомендуется для большинства текстовых данных

### 3. Что такое индекс и зачем он нужен? Назовите основные типы индексов
**Ответ:**
Индекс - это структура данных для ускорения операций чтения за счет дополнительного места на диске и времени на запись.

**Основные типы индексов:**
- **B-tree** - индекс по умолчанию, для операций сравнения и диапазонов
- **Hash** - для операций строгого равенства (=)
- **GIN** - для составных данных (массивы, JSONB, полнотекстовый поиск)
- **GiST** - для геоданных и диапазонов
- **BRIN** - для больших таблиц с физической упорядоченностью данных

---

## Средний уровень 🟡

### 4. Объясните разницу между INNER JOIN и LEFT OUTER JOIN
**Ответ:**
- **INNER JOIN** - возвращает только строки с совпадениями в обеих таблицах
- **LEFT OUTER JOIN** - возвращает все строки из левой таблицы и совпадающие из правой, при отсутствии совпадений правые столбцы заполняются NULL

### 5. Что такое транзакция? Объясните свойства ACID
**Ответ:**
Транзакция - это последовательность операций, выполняемых как единое целое.

**Свойства ACID:**
- **Atomicity (Атомарность)** - все операции транзакции выполняются или ни одна
- **Consistency (Согласованность)** - транзакция переводит БД из одного целостного состояния в другое
- **Isolation (Изолированность)** - параллельные транзакции не мешают друг другу
- **Durability (Долговечность)** - результаты завершенной транзакции сохраняются после сбоя

### 6. Объясните уровни изоляции транзакций в PostgreSQL
**Ответ:**
- **Read Committed** (по умолчанию) - транзакция видит только закоммиченные данные другими транзакциями
- **Repeatable Read** - транзакция видит снимок данных на момент своего первого запроса
- **Serializable** - самый строгий уровень, гарантирует последовательную выполнимость транзакций

### 7. В чем разница между UNION и UNION ALL?
**Ответ:**
- **UNION** - объединяет результаты двух запросов, удаляя дубликаты (требует сортировки)
- **UNION ALL** - объединяет все строки, включая дубликаты, работает быстрее

### 8. Что такое оконные функции? Приведите пример
**Ответ:**
Оконные функции выполняют вычисления across наборе строк, связанных с текущей строкой, без схлопывания строк.

**Пример:**
```sql
SELECT name, department, salary,
       RANK() OVER (PARTITION BY department ORDER BY salary DESC) as rank_in_dept
FROM employees;
```

---

## Продвинутый уровень 🟠

### 9. Как работает команда VACUUM в PostgreSQL?
**Ответ:**
`VACUUM` освобождает место, занимаемое "мертвыми" строками (удаленными или обновленными). В PostgreSQL при UPDATE или DELETE старые версии строк не удаляются сразу, а помечаются как невидимые. `VACUUM` очищает их, делая пространство доступным для новых записей.

### 10. В чем разница между json и jsonb? Когда использовать каждый?
**Ответ:**
- **json** - хранит точную копию JSON, быстрее на запись, медленнее на чтение
- **jsonb** - бинарное представление, медленнее на запись, быстрее на чтение, поддерживает индексы

**Рекомендация:** Использовать `jsonb` практически всегда, кроме случаев когда важна точная сохранность формата.

### 11. Что такое таблицы-последовательности и как они связаны с SERIAL?
**Ответ:**
Sequence - это объект базы данных, который генерирует последовательность уникальных целых чисел. Тип `SERIAL` - это удобное сокращение, которое:
1. Создает sequence
2. Устанавливает его как значение по умолчанию для столбца
3. Автоматически использует следующее значение при вставке

### 12. Как работает репликация в PostgreSQL? Какие виды вы знаете?
**Ответ:**
**Физическая репликация** - потоковая передача WAL-файлов, реплика является побайтовой копией мастера (только для чтения).

**Логическая репликация** - копирование изменений на уровне таблиц и строк, позволяет реплицировать только часть данных.

---

## Оптимизация и администрирование 🔴

### 13. Зачем нужен EXPLAIN и как его использовать?
**Ответ:**
`EXPLAIN` показывает план выполнения запроса. `EXPLAIN ANALYZE` выполняет запрос и показывает реальное время выполнения. Это основной инструмент для оптимизации запросов.

### 14. Что такое табличные пространства и зачем они нужны?
**Ответ:**
Tablespaces позволяют определять альтернативные расположения на диске для хранения таблиц, индексов и других объектов.

**Применение:**
- Разделение данных и индексов на разные физические диски
- Оптимизация производительности
- Управление дисковым пространством

### 15. Что такое блокировки и какие основные типы бывают?
**Ответ:**
Блокировки - это механизм, который предотвращает конфликтующие одновременные операции с одними и теми же данными.

**Основные типы:**
- **Table-Level Locks** - блокируют всю таблицу
- **Row-Level Locks** - блокируют отдельные строки
- **Advisory Locks** - прикладные блокировки, управляемые приложением

---

## Расширенные возможности 🟣

### 16. Что такое PATRONI и для чего он используется?
**Ответ:**
**PATRONI** - это решение для управления репликацией и автоматического фейловера в кластерах PostgreSQL. Он обеспечивает высокую доступность, автоматически переключая мастер при сбоях.

**Основные функции:**
- Автоматический фейловер
- Управление конфигурацией кластера
- Интеграция с системами обнаружения сервисов (Etcd, Consul, Zookeeper)
- Мониторинг состояния узлов

### 17. Как PATRONI обеспечивает высокую доступность?
**Ответ:**
**PATRONI** обеспечивает высокую доступность через:
1. **Распределенный консенсус** - использует DCS (Distributed Configuration Store) для выбора мастера
2. **Мониторинг здоровья** - постоянно проверяет состояние узлов
3. **Автоматический фейловер** - при сбое мастера автоматически promotes реплику
4. **Переключение ролей** - управляет переключением между мастером и репликами

### 18. Какие компоненты входят в архитектуру PATRONI?
**Ответ:**
**Основные компоненты:**
- **Patroni Daemon** - основной процесс на каждом узле PostgreSQL
- **DCS (Distributed Configuration Store)** - хранилище состояния кластера (Etcd/Consul/Zookeeper)
- **HAProxy/Keepalived** - для балансировки нагрузки и виртуального IP
- **Configuration Files** - настройки для каждого узла

**Рабочий процесс:**
1. Все узлы регистрируются в DCS
2. PATRONI выбирает мастер через распределенный консенсус
3. Реплики синхронизируются с мастером
4. При сбое мастера происходит автоматический перевыбор